/**
 * Created by SongJing on 2017/8/8.
 * 公用全局变量
 */


/*切片数据*/
var slicesObj = {
    axialObj: {
        slope: 1.0,
        intercept: 0,
        windowCenter: 35,
        windowWidth: 80,
        rows: 200,
        columns: 200,
        height: 200,
        width: 200,
        color: false,
        columnPixelSpacing: 1,
        rowPixelSpacing: 1,
        invert: false,
        canvsData:[],
        imageDataDraw: [],
        imageData: [],
        minPixelValue: -1000,
        maxPixelValue: 1000,
        sizeInBytes: 200 * 200 * 2
    },
    coronalObj: {
        slope: 1.0,
        intercept: 0,
        windowCenter: 35,
        windowWidth: 80,
        rows: 200,
        columns: 200,
        height: 200,
        width: 200,
        color: false,
        columnPixelSpacing: 1,
        rowPixelSpacing: 1,
        invert: false,
        canvsData:[],
        imageDataDraw: [],
        imageData: [],
        minPixelValue: -1000,
        maxPixelValue: 1000,
        sizeInBytes: 200 * 200 * 2
    },
    sagttialObj: {
        slope: 1.0,
        intercept: 0,
        windowCenter: 35,
        windowWidth: 80,
        rows: 200,
        columns: 200,
        height: 200,
        width: 200,
        color: false,
        columnPixelSpacing: 1,
        rowPixelSpacing: 1,
        invert: false,
        canvsData:[],
        imageDataDraw: [],
        imageData: [],
        minPixelValue: -1000,
        maxPixelValue: 1000,
        sizeInBytes: 200 * 200 * 2
    }
};

/*$.fn.RangeSlider = function(cfg){
    this.sliderCfg = {
        min: cfg && !isNaN(parseFloat(cfg.min)) ? Number(cfg.min) : null,
        max: cfg && !isNaN(parseFloat(cfg.max)) ? Number(cfg.max) : null,
        step: cfg && Number(cfg.step) ? cfg.step : 1,
        callback: cfg && cfg.callback ? cfg.callback : null
    };

    var $input = $(this);
    var min = this.sliderCfg.min;
    var max = this.sliderCfg.max;
    var step = this.sliderCfg.step;
    var callback = this.sliderCfg.callback;

    $input.attr('min', min)
        .attr('max', max)
        .attr('step', step);

    $input.bind("input", function(e){
        $input.attr('value', this.value);
        $input.css( 'background', 'linear-gradient(to right, #059CFA, white ' + this.value + '%, white)' );

        if ($.isFunction(callback)) {
            callback(this);
        }
    });
};*/


var sliderType='X';//旋转类型
// var pixelScale=10000;//像素值的放大倍数
// var pixelScale=10000;
